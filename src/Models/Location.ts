export class Location {
    id: string;
    name: string;
    state: string;
  
    constructor(id: string, name: string, state: string) {
      this.id = id;
      this.name = name;
      this.state = state;
    }
  }